/** @module access */

// eslint-disable-next-line import/prefer-default-export
export enum Access {
  UNKNOWN = 'unknown',
  USER = 'user',
  ORGANIZATION = 'organization',
  PUBLIC = 'public',
}

export default Access;
